{
    "authenticationMethod": "route",
    "routes": [
        {
            "source": "^/sales-order/(.*)",
            "target": "srv-api",
            "csrfProtection": true,
            "authenticationType": "xsuaa"
        }
    ]
}